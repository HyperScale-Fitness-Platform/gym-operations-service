apiVersion: v1
kind: Service
metadata:
  name: gym-operations-service
  namespace: gym-dev
spec:
  selector:
    app: gym-operations-service
  ports:
    - port: 4001
      targetPort: 4001
  type: ClusterIP